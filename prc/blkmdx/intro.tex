The \blkMdxMod{} module is a data module for the \inst{MODEXP} and \inst{BLAKE2f} precompiles.
It serves as a passage point for data extracted from \textsc{ram} to be funneled to the relevant \inst{MODEXP} and \inst{BLAKE2f} circuits.
It serves no purpose beyond storing data.
