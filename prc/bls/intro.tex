In $\blsMod$ module we detect a subset of the possible ways precompiles execution related to bls12 elliptic curves can fail. 

% TODO: extend this
We define the following shorthands:
\[
    \left\{
    \begin{array}{lcl}
        \blsPrime
         & \!\!\! = \!\!\! & \blsPrimeHex                                                                         \\
        \blsPrimeHi
         & \!\!\! = \!\!\! & \blsPrimeHexHi                                                                       \\
        \blsPrimeLo
         & \!\!\! = \!\!\! & \blsPrimeHexLo                                                                       \\
        % C_1
        % & \!\!\! = \!\!\! & \{ (\ttx, \tty) \in F_{\bnOuterPrime}^2 \mid \tty^2 = \ttx^3 + 3 \} \cup \{ (0, 0)\}
    \end{array}
    \right.
\]