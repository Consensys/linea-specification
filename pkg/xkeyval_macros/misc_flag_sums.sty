\makeatletter
% DEFINING KEYS

\define@cmdkey [MISCFLAGS] {var} {anchorRow} {}
\define@cmdkey [MISCFLAGS] {var} {relOffset} {}

% DEFAULT VALUES
\presetkeys [MISCFLAGS] {var} {
	anchorRow = \missingParameter,
	relOffset = \missingParameter,
}{}

\newcommand{\weightedMiscFlagSumExceptMmuName} {\miscSignifier\texttt{weightedMiscFlagSumSansMmu}}
\newcommand{\weightedMiscFlagSumExceptMmu} [1] {
	\setkeys[MISCFLAGS]{var}{#1}
	\weightedMiscFlagSumExceptMmuName _{\cmdMISCFLAGS@var@anchorRow}
	\big[\;\cmdMISCFLAGS@var@relOffset\;\big]
}

% \weightedMiscFlagSumExceptMmu {
% 	anchorRow = ,
% 	relOffset = ,
% }

\newcommand{\weightedMiscFlagSumName} {\miscSignifier\texttt{weightedMiscFlagSum}}
\newcommand{\weightedMiscFlagSum} [2] {
	\setkeys[MISCFLAGS]{var}{#1}
	\weightedMiscFlagSumName _{\cmdMISCFLAGS@var@anchorRow}
	\big[\;\cmdMISCFLAGS@var@relOffset\;\big]
}

% \weightedMiscFlagSum {
% 	anchorRow = ,
% 	relOffset = ,
% }


\makeatother
