\usepackage{listings}

\newcommand{\maxLog}                      {\col{maxlog()}}

% ==============================================================================
% Syntax Highlighting
% ==============================================================================

\lstdefinelanguage{zkasm}
{
        keywords={pub, fail, fn, goto, if, include, return, var},
        basicstyle=\ttfamily, 
        commentstyle=\small\sffamily\itshape\color{gray!80},
        stringstyle=\itshape,
        keywordstyle=\bfseries\color{darkgray},
        morecomment=[l][commentstyle]{;;},      % single line comments are set by...
        backgroundcolor=\color{lightgray!7},
        frame=single, % adds a frame around the code
        frameround=tttt,
        framesep=0.1cm,
        rulecolor=\color{gray},
        texcl,                                                          % ...LaTeX
        moredelim=[is][\ttfamily]{<code>}{</code>}, % allows setting code inside multi-line comments
        tabsize=4,
	tab=\rightarrowfill,
        xleftmargin=0.0cm,
        xrightmargin=0.0cm,
        showspaces=false,
        showtabs=false,
        columns=fullflexible,
        numberstyle=\tiny,
        escapechar=\%,
        keepspaces=true,
        mathescape=true
}

\lstset{
  language=zkasm
}
