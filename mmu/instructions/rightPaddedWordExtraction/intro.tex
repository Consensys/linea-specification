The \mmuMod{}-instruction \mmuInstRightPaddedWordExtraction{} instruction will only ever be called with parameters satisfying
\[
	\macroSrcOffsetLo < \macroRefSize.
\]
\ob{TODO ? We could add this as an extra constraint. This preselection is already done in the \hubMod{} but still \dots{}}
