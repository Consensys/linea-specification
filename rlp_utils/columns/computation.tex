\begin{enumerate}
    \item $\argOneHi$, $\argOneLo$, $\argTwoLo$, $\res$, $\INST$:
	arguments for calls to the \wcpMod{} module and $\maxLog$ function;
    \item $\wcpCtMax$ return value for calls to the $\maxLog$ function;
    \item $\shfFlag$ ,$\shfArg$, $\shfPower$:
	arguments for calls to the shift reference table;
    \item $\accumulator$ and $\limb$,  used only for the \inst{DATA\_PRICING} instruction
\end{enumerate}
