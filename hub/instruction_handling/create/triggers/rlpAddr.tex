We define
\[
	\locTriggerRlpAddr \define \scenCreateComputeAddress _{i}
\]
\saNote{} In other words: we systematically invoke the \rlpAddrMod{} module unless the \inst{CREATE}-type instruction either
(\emph{a}) raises an exception or
(\emph{b}) is aborted.

