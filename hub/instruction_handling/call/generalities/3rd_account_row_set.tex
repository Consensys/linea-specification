\begin{center}
	\boxed{%
		\text{The constraints presented below require that }
		\left\{ \begin{array}{lcl}
			\peekScenario                   _{i + \callFirstScenarioRowOffset } & = & 1 \\
			\scenCallRequiresAccountsThrice _{i + \callFirstScenarioRowOffset } & = & 1 \\
		\end{array} \right.
		}
\end{center}
The present section finishes the ``reverting/undoing'' of account operations in the
\[
	\scenCallRequiresAccountsThrice \equiv \true
\]
case.
Starting with \cite{EIP-7702} this operation affects both the
\calleeName{} and the
\delegateName{}.
What remains to be done is to undo the warmth update induced by the underlying \inst{CALL}-instruction.
\begin{description}
		\input{instruction_handling/call/generalities/rows/3rd_set/B___callee}
		\input{instruction_handling/call/generalities/rows/3rd_set/C___delegate}
\end{description}
