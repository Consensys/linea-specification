The present section introduces a number of notational shorthands relevant for \textbf{scenario-rows}.
As such these shorthands should \textbf{only} be used on row with row-index $i$ where $\peekScenario_{i} = 1$.
