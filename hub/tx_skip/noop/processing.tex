\input{tx_skip/noop/_precondition}
We now populate the rows of the initialization phase.
\begin{description}
	\item[\underline{\underline{Sanity checks:}}]
		we impose that
		\[
			\left\{ \begin{array}{lclr}
				\txSkip          _{i} & = & 1 & (\sanityCheck) \\
				\peekTransaction _{i} & = & 1 & (\sanityCheck) \\
			\end{array} \right.
		\]
		\input{tx_skip/noop/rows/transaction}
		\input{tx_skip/noop/rows/context_zero}
\end{description}
