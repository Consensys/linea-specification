\begin{enumerate}[resume]
	\item $\miscOobInst$:
		\oobMod{} instruction column, see chapter~(\ref{chap: oob});
	\item $\miscOobDataCol{k}$, $k = 1, 2, \dots, \numberOfOobDataColumns$:
		data columns which carry both ``trusted'' inputs and ``predictions'' to the \oobMod{} module;
\end{enumerate}
