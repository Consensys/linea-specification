\begin{center}
	\boxed{\text{All constraints in this subsection assume that }
		\left\{ \begin{array}{lcl}
			\txInit          _{i - 1}    & = & 0 \\
			\txInit          _{i}        & = & 1 \\
			\locFirstTransactionInBlock  & = & 1 \\
		\end{array} \right.
	}
\end{center}
We define the following shorthands
\[
	\left\{ \begin{array}{lcl}
		\locLayerOneTimeStampModuloPrime   & \define & \txLayerOneTimeStampModuloPrime   _{i + \locTxInitRoffForwardTxn} \\
		\locBeaconRootHi                   & \define & \txBeaconRootHi                   _{i + \locTxInitRoffForwardTxn} \\
		\locBeaconRootLo                   & \define & \txBeaconRootLo                   _{i + \locTxInitRoffForwardTxn} \\
		% \locPreviousBlockNumberModuloPrime & \define & \txPreviousBlockNumberModuloPrime _{i + \locTxInitRoffForwardTxn} \\
		% \locPreviousBlockHashHi            & \define & \txPreviousBlockHashHi            _{i + \locTxInitRoffForwardTxn} \\
		% \locPreviousBlockHashLo            & \define & \txPreviousBlockHashLo            _{i + \locTxInitRoffForwardTxn} \\ % XXXXXX with EIP 2935 \specTodo
	\end{array} \right.
\]
Under these conditions the \zkEvm{} elects to perform various
\textsc{system transactions}.
\begin{description}
		\input{tx_init/system/account_beaconroot}
		\input{tx_init/system/storage_beaconroot}
		% \input{tx_init/system/account_blockhash}
		% \input{tx_init/system/storage_blockhash} % XXXXXX \specTodo
\end{description}
