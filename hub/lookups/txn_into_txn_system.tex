The present section describes the lookup between the \hubMod{} and the \txnSystemMod{} module.
\begin{description}
	\item[Selector column:] we use the following selector: $\col{sel} \define \peekTransaction$
	\item[Source columns:] ---
		\begin{multicols}{2}
			\begin{enumerate}
				\item $\txLayerOneTimeStampModuloPrime$
				\item[\vspace{\fill}]
				\item $\txBeaconRootHi$
				\item $\txBeaconRootLo$
				% \item $\txPreviousBlockNumberModuloPrime$
				% \item $\txPreviousBlockHashHi$
				% \item $\txPreviousBlockHashLo$ % XXXXXX with EIP 2935 \specTodo
			\end{enumerate}
		\end{multicols}
	\item[Target columns:] ---
		\begin{multicols}{2}
			\begin{enumerate}
				\item $\txLayerOneTimeStampModuloPrime$
				\item[\vspace{\fill}]
				\item $\txBeaconRootHi$
				\item $\txBeaconRootLo$
				% \item $\txPreviousBlockNumberModuloPrime$
				% \item $\txPreviousBlockHashHi$
				% \item $\txPreviousBlockHashLo$ % XXXXXX with EIP 2935 \specTodo
			\end{enumerate}
		\end{multicols}
\end{description}
