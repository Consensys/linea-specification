\begin{center}
	\boxed{\text{All constraints in this subsection assume that }
	\left\{ \begin{array}{lcl}
		\txAuth          _{i + \roffAuthorizationProcessingFinalTransactionRow} & = & 1 \\
		\peekTransaction _{i + \roffAuthorizationProcessingFinalTransactionRow} & = & 1 \\
	\end{array} \right.
	}
\end{center}
We record the number of successful delegation tuples where the authority is the transaction sender:
\begin{enumerate}
	\item
		\If $\peekAuthorization _{i + \roffAuthorizationProcessingPreviousRow} = 1$
		we impose
		\[
			\left\{ \begin{array}{lcl}
				\txLengthOfDelegationList              _{i} & = & \authAuthorizationTupleIndex _{i + \roffAuthorizationProcessingLastAuthorityRowNearby} \\
				\txNumberOfSuccessfulSenderDelegations _{i} & = & \authSenderIsAuthorityAcc    _{i + \roffAuthorizationProcessingLastAuthorityRowNearby} \\
			\end{array} \right.
		\]
	\item
		\If $\peekAccount _{i + \roffAuthorizationProcessingPreviousRow} = 1$
		we impose
		\[
			\left\{ \begin{array}{lcl}
				\peekAuthorization                     _{i + \roffAuthorizationProcessingLastAuthorityRowRemote } & = & 1 \qquad (\sanityCheck)                                                                 \\
				\txLengthOfDelegationList              _{i + \roffAuthorizationProcessingFinalTransactionRow    } & = & \authAuthorizationTupleIndex _{i + \roffAuthorizationProcessingLastAuthorityRowRemote } \\
				\txNumberOfSuccessfulSenderDelegations _{i + \roffAuthorizationProcessingFinalTransactionRow    } & = & \authSenderIsAuthorityAcc    _{i + \roffAuthorizationProcessingLastAuthorityRowRemote } \\
			\end{array} \right.
		\]
\end{enumerate}

